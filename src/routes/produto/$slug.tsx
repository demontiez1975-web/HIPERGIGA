import { createFileRoute } from '@tanstack/react-router'
import { products } from '../../lib/demo-data'
export const Route=createFileRoute('/produto/$slug')({component:Page})
function Page(){const {slug}=Route.useParams();const p=products.find(x=>x.slug===slug);if(!p)return <main className="page"><div className="wrap"><h1>Produto não encontrado</h1></div></main>;return <main className="page"><div className="wrap detail"><div className="detail-visual">{p.emoji}</div><div><span className="eyebrow coral">{p.badge}</span><h1>{p.title}</h1><p>Produto demonstrativo da vitrine HIPERGIGA. Na etapa com Supabase, descrição, benefícios, imagens e link afiliado virão do banco.</p><strong className="price">{p.price.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}</strong><p className="muted">Loja parceira: {p.store}</p><a className="btn accent big" href="https://example.com" target="_blank" rel="sponsored noopener noreferrer">Ver oferta na loja</a><small className="disclosure">Podemos receber comissão por compras feitas por este link, sem custo adicional para você.</small></div></div></main>}
